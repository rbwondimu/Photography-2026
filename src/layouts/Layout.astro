---
interface Props {
	title: string;
    theme?: 'dark' | 'light';
}

const { title, theme = 'dark' } = Astro.props;
import Navigation from '../components/Navigation.astro';
---

<!doctype html>
<html lang="en" class={`theme-${theme}`}>
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Professional Photography Portfolio" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/png" href={import.meta.env.BASE_URL.replace(/\/$/, '') + "/fav-icon.png"} />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
	</head>
	<body>
		<Navigation theme={theme} />
		<slot />
	</body>
</html>
<style is:global>
	:root {
		--color-bg: #1a1a1a;
		--color-surface: #242424;
		--color-text: #f5f5f2;
		--color-accent: #c9a86a;
		--color-accent-hover: #dfc28e;
		--color-muted: #c9c9c2;
		--color-border: rgba(201, 168, 106, 0.28);
		--font-serif: 'Playfair Display', serif;
		--font-sans: 'Lato', sans-serif;
		--spacing-sm: 0.5rem;
		--spacing-md: 1rem;
		--spacing-lg: 2rem;
		--spacing-xl: 4rem;
	}

    .theme-light {
        --color-bg: #1a1a1a;
        --color-surface: #242424;
        --color-text: #f5f5f2;
        --color-accent: #c9a86a;
        --color-accent-hover: #dfc28e;
        --color-muted: #c9c9c2;
        --color-border: rgba(201, 168, 106, 0.28);
    }

	html {
		font-family: var(--font-sans);
		background: var(--color-bg);
		color: var(--color-text);
		scroll-behavior: smooth;
	}
	body {
		margin: 0;
		padding: 0;
		display: flex;
		flex-direction: column;
		min-height: 100vh;
	}
	h1, h2, h3, h4, h5, h6 {
		font-family: var(--font-serif);
		font-weight: 400;
		margin-top: 0;
	}
	a {
		color: inherit;
		text-decoration: none;
		transition: color 0.3s ease;
	}
	a:hover {
		color: var(--color-accent);
	}
</style>
