---
import Layout from '../layouts/Layout.astro';
---

<Layout title="About Me | NEW YEAR">
    <main>
        <section class="bio-section">
            <div class="profile-image">
                <!-- Placeholder for photographer image -->
            </div>
            <div class="bio-content">
                <h1>Behind the Lens</h1>
                <p class="intro">Hi, I'm the eye behind New Year Photography.</p>
                <p>
                    I believe that every photograph tells a story. My passion lies in capturing the fleeting moments that make life beautiful. 
                    From the quiet intimacy of a portrait to the joyous celebration of a wedding, I strive to create images that are honest, emotive, and timeless.
                </p>
                <p>
                    With a focus on natural light and genuine connection, my goal is to make you feel comfortable and confident, allowing your true self to shine through.
                </p>
            </div>
        </section>

        <section class="philosophy">
            <h2>My Philosophy</h2>
            <div class="philosophy-grid">
                <div class="item">
                    <h3>Authenticity</h3>
                    <p>Capturing real moments and genuine emotions.</p>
                </div>
                <div class="item">
                    <h3>Timelessness</h3>
                    <p>Creating images that will be cherished for generations.</p>
                </div>
                <div class="item">
                    <h3>Connection</h3>
                    <p>Building a relationship to understand your unique story.</p>
                </div>
            </div>
        </section>
    </main>
</Layout>

<style>
    main {
        width: 100%;
        min-height: 100vh;
        padding-top: 80px;
    }

    .bio-section {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: var(--spacing-xl);
        padding: var(--spacing-xl) 10%;
        min-height: 60vh;
    }

    .profile-image {
        width: 300px;
        height: 400px;
        background: #333; /* Placeholder */
        flex-shrink: 0;
        box-shadow: 20px 20px 0 var(--color-accent);
    }

    .bio-content {
        max-width: 500px;
    }

    .intro {
        font-size: 1.5rem;
        color: var(--color-accent);
        font-family: var(--font-serif);
        margin-bottom: var(--spacing-md);
    }

    p {
        line-height: 1.8;
        color: var(--color-text);
        margin-bottom: var(--spacing-md);
    }

    .philosophy {
        background: #222;
        padding: var(--spacing-xl) 10%;
        text-align: center;
    }

    .philosophy h2 {
        font-size: 2.5rem;
        margin-bottom: var(--spacing-xl);
    }

    .philosophy-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: var(--spacing-lg);
    }

    .item h3 {
        font-size: 1.2rem;
        color: var(--color-accent);
        margin-bottom: var(--spacing-sm);
    }

    @media (max-width: 768px) {
        .bio-section {
            flex-direction: column;
            text-align: center;
        }
        .profile-image {
            width: 100%;
            max-width: 300px;
            height: 300px;
            box-shadow: 10px 10px 0 var(--color-accent);
        }
    }
</style>
