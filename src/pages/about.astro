---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';
import profileImage from '../assets/portfolio/profile 2.jpg';
---

<Layout title="About Me | Wondimu Photography">
    <main>
        <section class="bio-section">
            <div class="profile-image-container">
                <Image
                    src={profileImage}
                    alt="Photographer Profile"
                    class="profile-image"
                    widths={[320, 480, 640]}
                    sizes="(max-width: 768px) 80vw, 300px"
                    format="webp"
                    quality={78}
                    loading="eager"
                    fetchpriority="high"
                />
            </div>
            <div class="bio-content">
                <h1>Behind the Lens</h1>
                <p class="intro">I'm Robel Wondimu, your brother.</p>
                <p>
                    I believe every photograph tells a story. My passion lies in capturing the fleeting moments that make life beautiful. From the quiet intimacy of a portrait to the joyful celebration of a wedding, and everything in between, I strive to create images that are authentic, captivating, and timeless.
                </p>
                <p>
                    Shot exclusively on film, my work preserves your most priceless memories with soulful depth, refined elegance, and enduring beauty. With a focus on natural light and genuine connection, I create a space where you feel at ease, allowing your true self to shine.
                </p>
            </div>
        </section>

        <section class="philosophy">
            <h2>My Philosophy</h2>
            <div class="philosophy-grid">
                <div class="item item-authenticity">
                    <h3>Authenticity</h3>
                    <p>
                        <span class="auth-line">Capturing real moments and genuine emotions.</span><br />
                        <span class="auth-line">The glance. The breath. The almost-missed moment.</span>
                    </p>
                </div>
                <div class="item item-timelessness">
                    <h3>Timelessness</h3>
                    <p>Soft grain, honest light, enduring beauty.<br /><span class="timeless-line">These are not just photographs, they are the beginning of your story.</span></p>
                </div>
                <div class="item item-connection">
                    <h3>Connection</h3>
                    <p>
                        <span class="connection-line-1">Each image is deliberate. No excess. No overshooting.</span><br />
                        <span class="connection-line">Every story is different and I take the time to understand yours.</span>
                    </p>
                </div>
            </div>
        </section>
    </main>
</Layout>

<style>
    main {
        width: 100%;
        min-height: 100vh;
        padding-top: 80px;
    }

    .bio-section {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: var(--spacing-xl);
        padding: calc(var(--spacing-xl) * 0.7) 10% calc(var(--spacing-lg) * 1.1);
        min-height: 60vh;
    }

    .profile-image-container {
        width: 300px;
        height: 400px;
        flex-shrink: 0;
        box-sizing: border-box;
        position: relative;
        padding: 10px;
        background: linear-gradient(145deg, #2b2b2b 0%, #242424 100%);
        border: 0.5px solid var(--color-border);
        border-radius: 10px;
        box-shadow:
            0 18px 36px rgba(0, 0, 0, 0.38),
            0 0 30px rgba(201, 168, 106, 0.28),
            0 0 0 1px rgba(245, 245, 242, 0.22) inset;
    }
    
    .profile-image {
        width: 100%;
        height: 100%;
        box-sizing: border-box;
        object-fit: cover;
        display: block;
        border-radius: 6px;
        background: rgba(36, 36, 36, 0.92);
        border: 0.5px solid var(--color-border);
        box-shadow: 0 0 0 1px rgba(245, 245, 242, 0.3) inset;
    }

    .bio-content {
        max-width: 760px;
        text-align: center;
    }

    .intro {
        font-size: 1.5rem;
        color: var(--color-accent);
        font-family: var(--font-serif);
        margin-bottom: var(--spacing-md);
    }

    .bio-content h1 {
        font-size: 2.5rem;
        margin-bottom: var(--spacing-md);
    }

    p {
        line-height: 1.8;
        color: var(--color-text);
        margin-bottom: var(--spacing-md);
    }

    .bio-content p:not(.intro) {
        text-align: center;
        font-size: 0.98rem;
        line-height: 1.6;
        margin-bottom: 0.9rem;
    }

    .philosophy {
        background: var(--color-bg);
        padding: calc(var(--spacing-md) * 0.7) 7% calc(var(--spacing-xl) * 0.75);
        text-align: center;
    }

    .philosophy h2 {
        font-size: 2.5rem;
        margin-bottom: calc(var(--spacing-md) + 1rem);
        text-align: center;
    }

    .philosophy-grid {
        display: grid;
        grid-template-columns: 1fr 1.55fr 1fr;
        grid-template-areas: "auth timeless connection";
        gap: calc(var(--spacing-md) * 1.2);
        align-items: start;
        max-width: 1260px;
        margin: 0 auto;
    }
    .philosophy .item {
        text-align: center;
    }
    .item-authenticity {
        grid-area: auth;
        justify-self: start;
    }
    .item-timelessness {
        grid-area: timeless;
        justify-self: center;
        max-width: 620px;
        width: 100%;
    }
    .item-connection {
        grid-area: connection;
        justify-self: end;
    }

    .item h3 {
        font-size: 1.5rem;
        color: var(--color-accent);
        margin-bottom: var(--spacing-sm);
        text-align: center;
    }
    .philosophy .item p {
        font-size: 0.96rem;
        line-height: 1.5;
        letter-spacing: 0.01em;
        margin-bottom: 0;
    }
    .timeless-line {
        display: inline-block;
        white-space: nowrap;
        font-size: 0.96rem;
    }
    .auth-line {
        display: inline-block;
        white-space: nowrap;
        font-size: 0.96rem;
    }
    .connection-line {
        display: inline-block;
        white-space: nowrap;
        font-size: 0.96rem;
    }
    .connection-line-1 {
        display: inline-block;
        white-space: nowrap;
        font-size: 0.96rem;
    }

    @media (max-width: 768px) {
        main {
            padding-top: 80px; /* Compact mobile nav */
        }
        .bio-section {
            flex-direction: column;
            text-align: center;
        }
        .bio-content p:not(.intro) {
            text-align: center;
        }
        .profile-image-container {
            width: 100%;
            max-width: 300px;
            height: auto;
            aspect-ratio: auto;
            overflow: visible;
            padding: 8px;
            border: 0.5px solid var(--color-border);
            border-radius: 8px;
            box-shadow:
                0 10px 20px rgba(0, 0, 0, 0.3),
                0 0 24px rgba(201, 168, 106, 0.24),
                0 0 0 1px rgba(245, 245, 242, 0.22) inset;
        }
        .profile-image {
            width: 100%;
            height: auto;
            object-fit: contain;
            object-position: center top;
            border-radius: 4px;
            border: none;
            box-shadow: 0 0 0 1px rgba(245, 245, 242, 0.3) inset;
        }
        .philosophy-grid {
            grid-template-columns: 1fr;
            grid-template-areas:
                "timeless"
                "auth"
                "connection";
            gap: var(--spacing-lg);
        }
        .item-authenticity,
        .item-timelessness,
        .item-connection {
            justify-self: center;
            max-width: none;
            width: 100%;
        }
        .philosophy {
            padding: var(--spacing-md) 7% var(--spacing-lg);
        }
        .philosophy h2 {
            margin-bottom: calc(var(--spacing-md) + 1rem);
        }
        .timeless-line {
            white-space: normal;
        }
        .auth-line {
            white-space: normal;
        }
        .connection-line {
            white-space: normal;
        }
        .connection-line-1 {
            white-space: normal;
        }
    }
</style>
