---
import Layout from '../layouts/Layout.astro';
import Gallery from '../components/Gallery.astro';

const categories = ["Portraits", "Weddings", "Events"];
---

<Layout title="Portfolio | Wondimu Photography">
    <main>
        <section class="gallery-section">
            <Gallery seamless={true} />
        </section>
    </main>
</Layout>

<script>
    // Simple client-side filtering logic (placeholder for now)
    const buttons = document.querySelectorAll('.filter-btn');
    buttons.forEach(btn => {
        btn.addEventListener('click', () => {
            // Remove active class from all
            buttons.forEach(b => b.classList.remove('active'));
            // Add to clicked
            btn.classList.add('active');
            
            // In a real implementation, this would filter the gallery items
            console.log(`Filtering by ${btn.getAttribute('data-filter')}`);
        });
    });
</script>

<style>
    main {
        padding-top: 80px; /* Offset for nav */
        min-height: 100vh;
        width: 100%;
        box-sizing: border-box;
    }

    @media (max-width: 768px) {
        main {
            padding-top: 130px; /* Taller stacked mobile nav */
        }
    }

    /* 
       Page header removed from markup, but styles kept just in case 
       or we can remove them. Let's keep main styles.
       
       For seamless look:
    */
    .gallery-section {
        padding: 0;
        width: 100%;
        max-width: 100%;
    }

    /* Enforce the same seamless vertical stack treatment used for Featured Work */
    .gallery-section :global(.gallery-grid.seamless) {
        display: flex;
        flex-direction: column;
        gap: 8px;
        padding: 8px 0;
    }

    .gallery-section :global(.gallery-grid.seamless .gallery-item) {
        width: 100%;
        height: auto;
    }

    .gallery-section :global(.gallery-grid.seamless .image-real) {
        width: 100%;
        height: auto;
        object-fit: contain;
        display: block;
    }
</style>
